#include <stdio.h>
#include <math.h>
int
main ()
{
  int n, c1, c2, b[16], i = 0;
  scanf("%d", &n);
  printf("%d", ((n >= 1) && (n <= 10)));
  cl = 1 / n;
  c2 = n / 10;
  printf("\n%d", cl & c2);
  printf("\n");
  int b[16];
  int ii = 0;
  int number = n;
  while(number > 0){
    bin[i]=abs (number % 2);
    number / = 2;
      i++;}
    for (int i = ii; i<16; ++i)
       bin[i] = 0;
    if (n < 0)
      b[15] = 1;
    printf("\n%d", b[3]);
    return 0;
  }
